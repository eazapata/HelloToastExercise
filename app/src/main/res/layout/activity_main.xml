<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:layout_gravity="center"
    tools:context=".MainActivity">

    <Button
        android:id="@+id/button_toast"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginLeft="30dp"
        android:layout_marginTop="30dp"
        android:layout_marginRight="30dp"
        android:background="@color/buttonColor"
        android:gravity="center"
        android:text="@string/buttonToastText"
        android:textColor="@color/buttonText" />

    <TextView
        android:id="@+id/show_count"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignTop="@+id/button_toast"
        android:layout_alignEnd="@+id/button_toast"
        android:layout_alignRight="@+id/button_toast"
        android:layout_marginTop="0dp"
        android:layout_marginEnd="-111dp"
        android:layout_marginRight="-111dp"
        android:background="@color/countTextView"
        android:text="@string/countContent"
        android:textColor="@color/buttonColor"
        android:textSize="200sp" />

    <Button
        android:id="@+id/button_count"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/button_toast"
        android:layout_marginLeft="30dp"
        android:layout_marginTop="0dp"
        android:layout_marginRight="0dp"
        android:background="@color/buttonColor"
        android:text="@string/buttonCountText"
        android:textColor="@color/buttonText" />
</RelativeLayout>